# ── 機密資訊（最優先）──────────────────────────────────────
# 競賽 Access Code 等憑證一律不入庫。原則是不寫進檔案，這幾條是後備防線。
*.local.md
*.local
.env
.env.local


# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/


# ── 壓力測試的中間產物 ────────────────────────────────────
stress/.tmp/


# ── 官方文件不進版控 ──────────────────────────────────────
# 唯讀證據、共約 103MB，且比賽當天會換成官方給的新檔。
# 位置用 VALUATION_DOC_DIR 指定，見 paths.py。
# 文字文件（.md）不受影響，照常進版控。
*.pdf
*.docx
*.xlsx


# ── 編輯器與作業系統 ──────────────────────────────────────
.DS_Store
.idea/


# ── 注意：/.kiro 絕對不可加入本檔案 ───────────────────────
# 競賽規範明文要求專案根目錄必須包含 /.kiro 以展示 specs、hooks、steering
# 的使用情況，並特別註明不得將其加入 .gitignore。

# ── 簡報與私人筆記不進版控 ──────────────────────────────────
# 投影片不是原始碼，而且內容會隨場合調整，放進來只會製造雜訊。
# 檔案保存在專案外的「私人筆記」目錄。
*.pptx
*.key
docs/pitch/
